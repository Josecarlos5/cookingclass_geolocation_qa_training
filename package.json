#### 2. `package.json`
```json
{
  "name": "cooking-class-qa-training",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.0.0",
    "typescript": "^4.0.0"
  }
}
